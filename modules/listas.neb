#modules/listas.neb
função elemento usando(list)
    conferir type(list) == "lista" usando "InvalidSyntax" detalhes "Esperava-se lista, não ${type(list)}"

    retornar pegar(list, aleatório(0, len(list) - 1))
fim